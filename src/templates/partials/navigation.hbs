<nav class="grid-nav -mt-6 md:-mt-10">
  <ul class="navbar bg-white list-reset text-xl md:text-2xl leading-loose md:flex md:justify-between">
    {{#foreach navigation}}
      <li class="mb-0 md:mb-6 md:text-center flex-1 nav-{{slug}}{{#if current}} active{{/if}}" role="presentation">
				<a class="nav-link md:px-4" href="{{url absolute="true"}}">
					{{label}}
				</a>
			</li>
    {{/foreach}}
    <li class="mb-0 md:mb-6 md:text-center flex-1">
			<a class="nav-link md:px-4" href="{{@blog.url}}/subscribe/">
				Subscribe
			</a>
		</li>
  </ul>
</nav>
