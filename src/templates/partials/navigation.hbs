<nav class="grid-nav -mt-2 mb-8">
  <ul class="navbar bg-white list-reset font-sans text-2xl leading-loose md:flex md:justify-between">
    {{#foreach navigation}}
      <li class="md:text-center flex-1 nav-{{slug}}{{#if current}} active{{/if}}" role="presentation">
				<a class="nav-link" href="{{url absolute="true"}}">
					{{label}}
				</a>
			</li>
    {{/foreach}}
    <li class="md:text-center flex-1">
			<a class="nav-link" href="{{@blog.url}}/subscribe/">
				Subscribe
			</a>
		</li>
  </ul>
</nav>
